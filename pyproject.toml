[project]
name = "deepocr"
version = "1.0.0"
description = "OCR multi-page PDFs using DeepSeek-OCR via Ollama"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pdf2image>=1.16.0",
    "Pillow>=10.0.0",
]

[dependency-groups]
dev = []
